on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to show the tag
    steps:
    - name: Hello world action step
      id: hello
      uses: klaudioz/testing_github_actions@master
      with:
        tag-name: 'Claudio'
